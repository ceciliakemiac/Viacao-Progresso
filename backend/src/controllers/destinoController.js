const fs = require('fs');
const _ = require('lodash');

let data = JSON.parse(fs.readFileSync('./server/data/destinoData.json'));

module.exports = {
    getAll(req, res) {
        
    },

    get(req, res) {
        
    },

    
}
