const fs = require('fs');

const data = JSON.parse(fs.readFileSync('./server/data/usuarioData.json'));

module.exports = {
    getAll(req, res) {
        
    },

    get(req, res) {
        
    },

    post(req, res) {
    
    },
}
